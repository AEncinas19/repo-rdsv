apiVersion: v1
kind: Service
metadata:
  name: {{ include "accesschart.fullname" . }}
  labels:
    {{- include "accesschart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
    - name: prom_node_exporter
      port: {{ .Values.service.prom_node_exporter.port}}
      targetPort: {{ .Values.service.prom_node_exporter.targetPort }}
      nodePort: {{ .Values.service.prom_node_exporter.nodePort }}
  selector:
    {{- include "accesschart.selectorLabels" . | nindent 4 }}
